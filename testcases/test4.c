#include<stdio.h>

int main()
{
	double a = 12.345;
	int mainvariable;
	"hello"
	int azb, zab, bza;
	"hello
	// Here, 12.345 is read as a float number and not as an integer(12) and float(.345)
	// This displays the rule priority of the lex analyser
	// mainvariable is read as an identifier and not a keyword.
}
